"use strict"

// Conditional operator

var dateA = 110;
var dateB = 20;

// Condition ? TRUE : FALSE
var result = dateA > dateB ? "Yes, is greater" : "No, is not greater";
console.log("The result with the conditional operator is: ", result);